import DeleteIcon from '@mui/icons-material/Delete';

// Button used inside BasketProduct to remove a product from the basket.

const RemoveButton = () => {
  return (
    <DeleteIcon className='basket-section-remove-button' /> 
  );
}

export default RemoveButton;
